{
  "name": "achieve-arcade",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix frontend && npm install --prefix backend",
    "heroku-postbuild": "npm run build --prefix frontend",
    "build": "npm run build --prefix frontend && npm run export --prefix frontend",
    "start": "node backend/server.js",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm start --prefix backend\""
  },
  "engines": {
    "node": "21.7.3"
  },
  "dependencies": {
    "concurrently": "^7.0.0",
    "express": "^4.17.1",
    "next": "latest",
    "react": "latest",
    "react-dom": "latest",
    "tailwindcss": "^2.0.0",
    "typescript": "^5.5.2"
  }
}
